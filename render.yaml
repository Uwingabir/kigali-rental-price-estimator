type: web
name: kigali-rental-estimation
env: python
plan: free
region: oregon
buildCommand: pip install -r requirements.txt && python train_model.py && python generate_notebook.py
startCommand: gunicorn app:app
autoDeploy: true
